export enum RA_RANGE_ENUM {
  min = 2000000,
  max = 3000000,
}

export enum PROJECT_STATUS_ENUM {
  inProgress = 'Em andamento',
  completed = 'Concluído',
  pending = 'Pendente',
  canceled = 'Cancelado',
}
